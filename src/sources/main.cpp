#include <triangle.hpp>
#include <bounding-volume.hpp>

#include <iostream>

int main() {
	cout << "size of bounding volume in bytes " << sizeof(BoundingVolume) << endl;

	system("pause");
	return 0;
}